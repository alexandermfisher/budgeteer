# =============================================================================
# TESTCONTAINERS CONFIGURATION
# =============================================================================
# Enable container reuse to speed up test execution
# This keeps containers running between test runs
testcontainers.reuse.enable=true

# Disable Ryuk (resource reaper) checks for faster startup
# Warning: Only use this if you're managing container cleanup manually
# or running tests in CI where cleanup happens automatically
checks.disable=false
